[build-system]
requires = ["maturin>=1.6,<2.0"]
build-backend = "maturin"

[project]
name = "theseo-core"
version = "0.1.0"
description = "Rust simulation and rendering bindings for Theseo AnySearch"
requires-python = ">=3.10"

[tool.maturin]
bindings = "pyo3"
module-name = "theseo_core"
