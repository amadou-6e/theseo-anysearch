# Replacement P1 terminal report

Run `voxel-encoder-pilot-v2r1-p1-1` is `not_started`. P0D produced no
retained calibrated objective bundle, so starting a replacement objective
screen would violate the amended preregistration.

No model was trained: trials, optimizer updates, and accelerator hours are all
zero. The v1 P1 `no_viable_direction` decision remains recorded as superseded
because its calibration contract was invalidated; this report does not replace
it with new P1 evidence.

The machine-readable decision is in `p1-terminal-report.json`.
